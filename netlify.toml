[build]
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "20"

# Fix client-side routing (VERY important for React apps)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
